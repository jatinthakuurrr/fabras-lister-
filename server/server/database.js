 import Database from 'better-sqlite3'; import path from 'path'; import { fileURLToPath} from 'url'; const __dirname = path.dirname(fileURLToPath(import.meta.url)); const db = new Database(path.join(__dirname, 'lister.db')); db.pragma('journal_mode = WAL'); export function initDatabase() { db.exec(CREATE TABLE IF NOT EXISTS products (...); CREATE TABLE IF NOT EXISTS import_history (...); CREATE TABLE IF NOT EXISTS settings (...);); } export default db;
